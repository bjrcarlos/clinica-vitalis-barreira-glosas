# Workflow Ralph — Fase 5

Entrada limpa: PRD §33/Fase 5, todos os handoffs, README e auditoria de segurança.

Loop executado: portão final → documentação/Skill/roteiro → diff/secrets/Git → verificação de
links/MCP → decisão de deploy.

Gate: aprovado para entrega local e demonstração. Deploy remoto permanece preparado, mas não
executado: `database_id` é placeholder e a publicação exige autorização explícita do proprietário.

Evidências: `phase-5-handoff.md`, `README.md`, `docs/CONFIG.md`, `git diff --check`, portões
`typecheck/test/build` e demonstrações HTTP/MCP.
