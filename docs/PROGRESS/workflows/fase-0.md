# Workflow Ralph — Fase 0

Entrada limpa: PRD §33/Fase 0, `CLAUDE.md`, estado do repositório e materiais oficiais.

Loop executado: inventário → scaffold/bindings/migração → typecheck/test/build → health local →
auditoria de Git/secrets → handoff.

Gate: aprovado com ressalva histórica: o índice atual bloqueia material privado, mas o histórico
contém o screenshot apontado no `STATE.md`; deploy público exige decisão do dono.

Evidências: `phase-0-handoff.md`, `wrangler.jsonc`, `migrations/0001_init.sql`, `docs/CONFIG.md`.
