# Workflow Ralph — Fase 1

Entrada limpa: PRD §33/Fase 1, handoff da Fase 0, motor/parser/regras e seed oficial.

Loop executado: parser/normalização → motor puro → persistência/seed → reconciliação da baseline
→ correção de duplicidade e ordem de cadastro → testes e seed repetível.

Gate: aprovado. 80 guias, 29 OK, 12 corrigir, 34 revisão humana, 5 não faturar e risco inicial
R$ 3.726,00; duplicidade vigente = 2 pares e tarefas = 70.

Evidências: `phase-1-handoff.md`, `docs/BASELINE.md`, `scripts/seed.ts`.
